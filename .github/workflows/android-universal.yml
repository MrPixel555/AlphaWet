name: Build AlphaWet Android APK

on:
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    env:
      BUILD_ROOT: /tmp/alphawet
      AW_INCLUDE_X86_64: '1'

      ORG_GRADLE_PROJECT_PLAY_CLOUD_PROJECT_NUMBER: '939996477634'
      ORG_GRADLE_PROJECT_EXPECTED_SIGNING_CERT_SHA256: '47b4e06477796fbbeecaf03ce8318d5bd1744115bf40590f918fd9d2d5a8df47'

      AW_ANDROID_KEYSTORE_PASSWORD: "K7m9Lp1R_x2!'"
      AW_ANDROID_KEY_PASSWORD: "Q5v8Tn3S_y4!'"
      AW_ANDROID_KEY_ALIAS: 'alphawet'

      AW_ANDROID_KEYSTORE_BASE64: ''

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Show tool versions
        run: |
          flutter --version
          dart --version
          java -version

      - name: Validate required signing inputs exist
        run: |
          test -n "$AW_ANDROID_KEYSTORE_BASE64"
          test -n "$AW_ANDROID_KEYSTORE_PASSWORD"
          test -n "$AW_ANDROID_KEY_ALIAS"
          test -n "$ORG_GRADLE_PROJECT_EXPECTED_SIGNING_CERT_SHA256"
          test -n "$ORG_GRADLE_PROJECT_PLAY_CLOUD_PROJECT_NUMBER"

      - name: Prepare Android project for CI
        env:
          AW_EXPECTED_SIGNING_CERT_SHA256: ${{ env.ORG_GRADLE_PROJECT_EXPECTED_SIGNING_CERT_SHA256 }}
        run: bash tool/ci_prepare_android_project.sh "$GITHUB_WORKSPACE" "$BUILD_ROOT"

      - name: Restore release keystore
        shell: bash
        run: |
          mkdir -p "$RUNNER_TEMP/alphawet-signing"
          printf '%s' "$AW_ANDROID_KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/alphawet-signing/release.jks"
          test -s "$RUNNER_TEMP/alphawet-signing/release.jks"
          printf 'AW_ANDROID_KEYSTORE_PATH=%s\n' "$RUNNER_TEMP/alphawet-signing/release.jks" >> "$GITHUB_ENV"

      - name: Validate keystore, alias, and resolve working key password
        shell: bash
        run: |
          KEYSTORE_PATH="$RUNNER_TEMP/alphawet-signing/release.jks"

          keytool -list -v \
            -keystore "$KEYSTORE_PATH" \
            -storepass "$AW_ANDROID_KEYSTORE_PASSWORD" \
            -alias "$AW_ANDROID_KEY_ALIAS" >/dev/null

          PROBE_DIR="$RUNNER_TEMP/alphawet-signing/probe"
          mkdir -p "$PROBE_DIR"
          printf 'alphawet signing probe\n' > "$PROBE_DIR/probe.txt"
          (
            cd "$PROBE_DIR"
            jar --create --file probe.jar probe.txt >/dev/null
          )

          try_sign() {
            local candidate="$1"
            jarsigner \
              -keystore "$KEYSTORE_PATH" \
              -storepass "$AW_ANDROID_KEYSTORE_PASSWORD" \
              -keypass "$candidate" \
              "$PROBE_DIR/probe.jar" \
              "$AW_ANDROID_KEY_ALIAS" >/dev/null 2>&1
          }

          if [ -n "${AW_ANDROID_KEY_PASSWORD:-}" ] && try_sign "$AW_ANDROID_KEY_PASSWORD"; then
            RESOLVED_KEY_PASSWORD="$AW_ANDROID_KEY_PASSWORD"
            echo "[OK] Private key password is valid."
          elif try_sign "$AW_ANDROID_KEYSTORE_PASSWORD"; then
            RESOLVED_KEY_PASSWORD="$AW_ANDROID_KEYSTORE_PASSWORD"
            echo "[WARN] Falling back to keystore password for private key."
          else
            echo "[ERROR] Neither AW_ANDROID_KEY_PASSWORD nor AW_ANDROID_KEYSTORE_PASSWORD could unlock alias '$AW_ANDROID_KEY_ALIAS'." >&2
            exit 1
          fi

          printf 'RESOLVED_AW_ANDROID_KEY_PASSWORD=%s\n' "$RESOLVED_KEY_PASSWORD" >> "$GITHUB_ENV"

      - name: Force unsigned release packaging in generated Android project
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import os

          app_dir = Path(os.environ["BUILD_ROOT"]) / "android" / "app"
          kts = app_dir / "build.gradle.kts"
          groovy = app_dir / "build.gradle"

          def patch_kts(path: Path):
              text = path.read_text()
              text = text.replace(
                  'signingConfig = signingConfigs.getByName("debug")',
                  '// release signing is performed later in CI with zipalign + apksigner'
              )
              text = text.replace(
                  'signingConfig = signingConfigs.getByName("release")',
                  '// release signing is performed later in CI with zipalign + apksigner'
              )
              path.write_text(text)

          def patch_groovy(path: Path):
              text = path.read_text()
              text = text.replace(
                  'signingConfig = signingConfigs.debug',
                  '// release signing is performed later in CI with zipalign + apksigner'
              )
              text = text.replace(
                  'signingConfig = signingConfigs.release',
                  '// release signing is performed later in CI with zipalign + apksigner'
              )
              path.write_text(text)

          if kts.exists():
              patch_kts(kts)
          elif groovy.exists():
              patch_groovy(groovy)
          else:
              raise SystemExit('No app build.gradle(.kts) found in generated Android project')
          PY

      - name: Install dependencies
        working-directory: ${{ env.BUILD_ROOT }}
        run: flutter pub get

      - name: Run tests
        working-directory: ${{ env.BUILD_ROOT }}
        run: flutter test

      - name: Static analysis
        working-directory: ${{ env.BUILD_ROOT }}
        run: flutter analyze --no-fatal-infos

      - name: Compose optional dart-define flags
        run: |
          args=()
          printf 'DART_DEFINE_ARGS=%s\n' "${args[*]}" >> "$GITHUB_ENV"

      - name: Build unsigned release APK
        working-directory: ${{ env.BUILD_ROOT }}
        run: flutter build apk --release --obfuscate --split-debug-info=build/symbols ${DART_DEFINE_ARGS}

      - name: Align and sign release APK with apksigner
        shell: bash
        run: |
          ZIPALIGN_BIN="$(find "${ANDROID_HOME}" -type f -name zipalign | sort -V | tail -n 1)"
          APKSIGNER_BIN="$(find "${ANDROID_HOME}" -type f -name apksigner | sort -V | tail -n 1)"
          test -n "$ZIPALIGN_BIN"
          test -n "$APKSIGNER_BIN"

          UNSIGNED_APK=""
          for candidate in \
            "$BUILD_ROOT/build/app/outputs/flutter-apk/app-release-unsigned.apk" \
            "$BUILD_ROOT/build/app/outputs/flutter-apk/app-release.apk" \
            "$BUILD_ROOT/build/app/outputs/apk/release/app-release-unsigned.apk" \
            "$BUILD_ROOT/build/app/outputs/apk/release/app-release.apk"
          do
            if [ -f "$candidate" ]; then
              UNSIGNED_APK="$candidate"
              break
            fi
          done

          if [ -z "$UNSIGNED_APK" ]; then
            echo "[ERROR] Could not locate release APK to sign." >&2
            find "$BUILD_ROOT/build/app/outputs" -maxdepth 4 -type f -name '*.apk' -print >&2 || true
            exit 1
          fi

          SIGN_DIR="$RUNNER_TEMP/alphawet-signing/out"
          mkdir -p "$SIGN_DIR"
          ALIGNED_APK="$SIGN_DIR/app-release-aligned.apk"
          FINAL_APK="$BUILD_ROOT/build/app/outputs/flutter-apk/app-release.apk"

          "$ZIPALIGN_BIN" -p -f 4 "$UNSIGNED_APK" "$ALIGNED_APK"

          printf '%s' "$AW_ANDROID_KEYSTORE_PASSWORD" > "$RUNNER_TEMP/alphawet-signing/ks-pass.txt"
          printf '%s' "$RESOLVED_AW_ANDROID_KEY_PASSWORD" > "$RUNNER_TEMP/alphawet-signing/key-pass.txt"

          "$APKSIGNER_BIN" sign \
            --ks "$AW_ANDROID_KEYSTORE_PATH" \
            --ks-key-alias "$AW_ANDROID_KEY_ALIAS" \
            --ks-pass "file:$RUNNER_TEMP/alphawet-signing/ks-pass.txt" \
            --key-pass "file:$RUNNER_TEMP/alphawet-signing/key-pass.txt" \
            --out "$FINAL_APK" \
            "$ALIGNED_APK"

          test -s "$FINAL_APK"

      - name: Verify release certificate pin
        shell: bash
        run: |
          APKSIGNER_BIN="$(find "${ANDROID_HOME}" -type f -name apksigner | sort -V | tail -n 1)"
          test -n "$APKSIGNER_BIN"
          VERIFY_OUTPUT="$($APKSIGNER_BIN verify --verbose --print-certs "$BUILD_ROOT/build/app/outputs/flutter-apk/app-release.apk" 2>&1)"
          printf '%s\n' "$VERIFY_OUTPUT"
          ACTUAL_CERT_SHA256="$(printf '%s\n' "$VERIFY_OUTPUT" | sed -nE 's/.*certificate SHA-256 digest: *([0-9A-Fa-f:]+).*/\1/p' | head -n 1 | tr -d ':\r\n ')"
          echo "Actual cert SHA256: $ACTUAL_CERT_SHA256"
          if [ -z "$ACTUAL_CERT_SHA256" ]; then
            echo "[ERROR] Could not parse certificate SHA-256 digest from apksigner output." >&2
            exit 1
          fi
          test "$ACTUAL_CERT_SHA256" = "$ORG_GRADLE_PROJECT_EXPECTED_SIGNING_CERT_SHA256"

      - name: Collect build outputs
        run: |
          mkdir -p dist
          cp "$BUILD_ROOT/build/app/outputs/flutter-apk/app-release.apk" dist/alphawet-universal-release.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: alphawet-universal-apk
          path: dist/alphawet-universal-release.apk
          if-no-files-found: error
